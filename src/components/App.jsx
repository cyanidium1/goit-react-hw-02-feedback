import Feedback from "./Feedback";

export const App = () => {
  return (
    <div>
      <Feedback
        props={'kek'}
        // username={username}
        // tag={tag}
        // location={location}
        // avatar={avatar}
        // stats={stats}
      />
    </div>
  );
};
